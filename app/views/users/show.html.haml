%h3 User

%div 
  Name: 
  %b= @user.name if @user.name

%div
  Email: 
  %b= @user.email if @user.email

- if user_signed_in? && @user == current_user
  %br
  %div
    =link_to('Sign out', destroy_user_session_path, method: :delete)
